{
    "enabled": {{ enabled }},
    "host": "{{ host }}",
    "port": {{ port }},
    "auth": ["{{ username }}", "{{ password }}"],
    "use_ssl": {{ use_ssl }},
    "ca_certs_path": "{{ ca_certs_path }}"
}
