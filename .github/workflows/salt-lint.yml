name: salt-lint
on: 
  schedule:
    - cron:  '0 3 * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: salt-lint
      uses: roaldnefs/salt-lint-action@v0.0.1
      env:
        ACTION_STATE_NAME: salt/top.sls
